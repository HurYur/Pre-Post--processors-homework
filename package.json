{
  "name": "Pre-Post--processors-homework",
  "scripts": {
    "build:html": "pug index.pug --pretty -o dest/",
    "build:css": "node-sass main.scss -o dest/css/",
    "build": "pug index.pug --pretty -o dest/ && node-sass main.scss -o dest/css/"
  },
  "dependencies": {
    "htmlhint": "^0.9.13",
    "node-sass": "^3.13.0",
    "pug": "^2.0.0-beta6",
    "pug-cli": "^1.0.0-alpha6",
    "stylelint": "^7.6.0"
  }
}
